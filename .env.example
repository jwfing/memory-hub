# Memory Hub Configuration Example
# Copy this file to .env and update with your actual values

# ============================================
# Database Configuration
# ============================================
POSTGRES_HOST=localhost
POSTGRES_PORT=5632
POSTGRES_DB=memhub
POSTGRES_USER=postgres
POSTGRES_PASSWORD=itsnothing

# ============================================
# Embedding Model Configuration
# ============================================
# Choose one of the following models:
#
# 1. paraphrase-multilingual-mpnet-base-v2 (Default - High quality, Multilingual)
#    - Dimensions: 768
#    - Size: ~970MB
#    - Languages: Chinese, English, and more
#    - Speed: Slower
#    - Quality: Excellent
#
# 2. paraphrase-multilingual-MiniLM-L12-v2 (Fast, Multilingual)
#    - Dimensions: 384
#    - Size: ~120MB
#    - Languages: Chinese, English, and more
#    - Speed: Fast
#    - Quality: Good
#
# 3. all-mpnet-base-v2 (High quality, English only)
#    - Dimensions: 768
#    - Size: ~420MB
#    - Languages: English only
#    - Speed: Medium
#    - Quality: Excellent
#
# 4. all-MiniLM-L6-v2 (Fast, English only)
#    - Dimensions: 384
#    - Size: ~80MB
#    - Languages: English only
#    - Speed: Very fast
#    - Quality: Good

EMBEDDING_MODEL=paraphrase-multilingual-mpnet-base-v2
EMBEDDING_DIMENSIONS=768

# IMPORTANT: If you change the embedding model, you MUST:
# 1. Update EMBEDDING_DIMENSIONS to match the model
# 2. Drop and recreate the database (embeddings are not compatible)

# ============================================
# MCP Server Configuration
# ============================================
MCP_SERVER_NAME=memory-hub
MCP_SERVER_VERSION=1.0.0

# ============================================
# Proxy Server Configuration (for server.py)
# ============================================
# HTTP backend URL (used by stdio proxy server)
MEMHUB_HTTP_URL=http://localhost:8000

# Authentication token for HTTP backend (get this after registering/logging in)
# Leave empty if authentication is not required
# Example: MEMHUB_AUTH_TOKEN=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
MEMHUB_AUTH_TOKEN=

# ============================================
# Authentication & Security
# ============================================
# IMPORTANT: Change this in production!
JWT_SECRET=change-this-secret-key-in-production-use-a-long-random-string
JWT_ALGORITHM=HS256
# Token expiration in hours (default: 168 = 7 days)
JWT_EXPIRATION_HOURS=168

# ============================================
# Quick Start Examples
# ============================================

# Example 1: Fast multilingual setup (recommended for getting started)
# EMBEDDING_MODEL=paraphrase-multilingual-MiniLM-L12-v2
# EMBEDDING_DIMENSIONS=384

# Example 2: High quality multilingual setup (current default)
# EMBEDDING_MODEL=paraphrase-multilingual-mpnet-base-v2
# EMBEDDING_DIMENSIONS=768

# Example 3: Fast English-only setup
# EMBEDDING_MODEL=all-MiniLM-L6-v2
# EMBEDDING_DIMENSIONS=384

# Example 4: High quality English-only setup
# EMBEDDING_MODEL=all-mpnet-base-v2
# EMBEDDING_DIMENSIONS=768